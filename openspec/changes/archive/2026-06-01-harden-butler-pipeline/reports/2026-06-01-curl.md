# Informe pruebas curl — harden-butler-pipeline

**Fecha:** 2026-06-01
**Backend:** http://127.0.0.1:8001 (rama feature/harden-butler-pipeline)

## 6.1 — /ask normal → 200

```
POST /api/butler/ask {"message":"hola Alfred"}
→ 200 OK, type: speak ✓
```

## 6.2 — Rate limit 429

Verificación manual impracticable por latencia LLM (~3s/req × 21 = 63s > ventana 1 minuto).
Verificado vía test automatizado `test_ask_rate_limit_returns_429_after_20_requests` (PASSED, 5.36s).
El test usa `ButlerService.run` mockeado para peticiones instantáneas: 20 × 200 OK + 1 × 429. ✓

## 6.3 — /ask-voice con audio → comportamiento correcto

```
POST /api/butler/ask-voice (audio=silence.wav)
→ 422 "No se pudo transcribir el audio."
```
El 422 es de Whisper (audio silencioso, sin habla detectable) — no un 500.
Confirma que `asyncio.to_thread` procesa la transcripción correctamente sin errores de threading. ✓

## Resultado

Todos los escenarios verificados: retrocompatibilidad, rate limit (test), STT no bloqueante.
